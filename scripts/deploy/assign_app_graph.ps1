<#
Assigns the Win32 app to Azure AD groups via Microsoft Graph.
Replace group IDs safely. Do not store secrets.
#>

# Connect-MgGraph -Scopes "DeviceManagementApps.ReadWrite.All"

Write-Host "This script is a placeholder. Implement Graph calls to assign the app (required/uninstall)."
Write-Host "Use assignment intent 'required' for pilot group, expand later for broad ring."
